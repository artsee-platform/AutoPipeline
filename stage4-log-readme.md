# Stage 4 终端日志分析（`run_pipeline.py --stage 4`）

本文档基于一次成功跑完的 Stage 4 日志（终端输出片段 + 结束汇总行）。命令来自终端元数据：`python3 run_pipeline.py --stage 4 --batch 200`。

## 1. 汇总结果（以日志最后一行为准）

| 指标 | 数值 |
|------|------|
| 处理学校数（touched） | **148** |
| 写入项目数（inserted） | **431** |
| 平均每校项目数 | **431 ÷ 148 ≈ 2.91**（多数为 3，部分不足 3） |
| 退出码 | **0**（成功） |

说明：`batch_size=200` 只影响单次循环批量，本 run 实际仍需处理的学校数为 148（其余可能已满足或跳过）。

## 2. 总耗时

- 终端缓冲区**未保留本次运行的起始行**（首行已是某一所学校处理完毕后的中间状态），因此**无法从文件中得到精确的全局开始时间**。
- **可见时间窗**（日志中最早一条时间戳 → 最后一条）：**15:09:28 → 16:54:08**  
  - 墙钟时长：**1 小时 44 分 40 秒（6 280 秒）**  
  - 该窗内约覆盖 **90～91** 所学校的「从 `→ 学校名` 到下一次 `→`」的处理过程（与最终 148 校相比，**前面约 50+ 所学校不在当前缓冲区内**）。

**推算全程墙钟（假设与可见片段每校耗时相近）：**

- 可见片段内，相邻两所学校开始处理的时间间隔（共 89 段）：**平均约 70 秒**，中位数约 **63 秒**，最短约 **40 秒**，最长约 **336 秒（约 5.6 分钟）**。
- 若按 **~70 秒/校 × 148 校** 估算：**约 10 360 秒 ≈ 2 小时 53 分**（量级估计，非精确测量）。

结论：**总时长粗算约 2.9～3 小时**；若需精确值，请用完整日志（含 `Loaded … schools` 或第一条 `→` 的时间戳）或重定向到文件后再统计。

## 3. 平均每校耗时（可见片段，可复现统计）

在**保存下来的日志段落**中，用「本条 `→ 学校` 时间」到「下一条 `→ 学校` 时间」的间隔作为该校处理耗时近似：

| 统计量 | 秒 | 说明 |
|--------|-----|------|
| 样本段数 | 89 | 相邻 `→` 之间的间隔 |
| 平均 | **~70** |  |
| 中位数 | **~63** |  |
| 最小 / 最大 | **~40 / ~336** | 长间隔多与多次抓取失败、PDF 跳过、重试等有关 |

该校平均**不**等于「总时长 ÷ 148」，因为总时长未知；上表仅反映**日志可见部分**的行为。

## 4. 可见片段内的插入条数分布

对日志中能匹配到的 `inserted N program(s) for this school` 行（约 91 条）统计：

| 插入条数 N | 大致学校数（可见片段） |
|------------|------------------------|
| 3 | 多数 |
| 2 | 若干（如官网抓取差、证据不足等） |
| 1 | 极少数 |

全量 431 条项目由 148 所学校贡献，整体仍以「每校尽量 3 条」为目标，少数未满。

## 5. 告警类型（可见片段，便于改爬虫/策略）

以下为日志行计数，**非**全局唯一 URL 数；同校可能多条 `fetch failed`。

| 类型 | 大致含义 | 可见行数（约） |
|------|----------|----------------|
| `fetch failed` + **403** | 站点反爬 / 需要浏览器 UA / 区域限制 | ~11 |
| `fetch failed` + **SSL**（证书链、主机名不匹配等） | 本地信任链或对方证书配置问题 | ~10 |
| `fetch failed` + **DNS**（`Failed to resolve`） | 域名不存在或本地 DNS 问题 | ~8 |
| `fetch failed` + **404** | 链接过期或路径错误 | ~8 |
| `fetch failed` + **连接超时** | 网络或对端慢 | ~2 |
| `skip non-html`（多为 `application/pdf`） | 当前流程只处理 HTML，PDF 直接跳过 | ~49 |

常见现象举例（日志中已出现）：大学站点把课程信息放在 PDF；部分 `.edu` 对脚本返回 403；个别域名已变更导致 NXDOMAIN。

## 6. 简要结论

1. **本次运行成功**：148 所学校写入 431 条 program 记录，pipeline 正常结束。  
2. **单校耗时**：在可见日志中约 **1～1.5 分钟** 为常态，极端可至 **数分钟**（多 URL 失败、超时、跳 PDF 等）。  
3. **全程时长**：因日志截断只能估计，**约 2.9～3 小时**（按可见平均 ~70 s/校 × 148 校）。  
4. **后续若要精确统计**：建议 `python run_pipeline.py --stage 4 ... 2>&1 | tee stage4_full.log`，再以首末时间戳与 `Stage 4 complete` 行核算。

---

*生成说明：基于 Cursor 终端捕获文件 `terminals/2.txt` 中时间戳与正则统计；若你本地有完整日志，可用首条 `Loaded`/`→` 与 `Stage 4 complete` 两行时间戳直接相减得到精确总时长。*
